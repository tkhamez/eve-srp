{% extends "layout.twig" %}
{% set active_page = "request" %}

{% block content %}

    <div class="row mb-3">
        <div class="col">
            <h1>Request</h1>
        </div>
    </div>

    {{ flashMessages()|raw }}

    {% if request %}
        <div class="page-request row mb-3">
            <div class="col-12 col-lg-7">
                <div class="card bg-dark text-light mb-3 border-light">
                    <div class="card-body">
                        {% include 'pages/request--data.twig' with { 'request': request } only %}
                    </div>
                </div>
                <div class="card bg-dark text-light border-light mt-3 " style="width:60%; margin-left:auto;">
                    <div class="card-body">
                        {% include 'pages/request--payouts.twig' with { 'currentPayouts': currentPayouts} only %}
                    </div>
                </div>
            </div>
            <div class="col-12 col-lg-5">
                <div class="card bg-dark text-light border-light">
                    <div class="card-body">
                        {% include 'pages/request--ship.twig' with {
                            'shipName': request.ship,
                            'shipTypeId': shipTypeId,
                            'items': items,
                            'killError': killError,
                        } only %}
                    </div>
                </div>
            </div>
        </div>
    {% endif %}

{% endblock %}
